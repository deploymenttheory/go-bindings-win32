// Code generated by go-bindings-win32-codegen. DO NOT EDIT.

//go:build windows && (amd64 || arm64)

package multimedia

import (
	"unsafe"

	"github.com/deploymenttheory/go-bindings-win32/bindings/runtime/win32"
	"github.com/deploymenttheory/go-bindings-win32/bindings/win32/foundation"
	graphicsgdi "github.com/deploymenttheory/go-bindings-win32/bindings/win32/graphics/gdi"
	mediamultimedia "github.com/deploymenttheory/go-bindings-win32/bindings/win32/media/multimedia"
	uicontrolsdialogs "github.com/deploymenttheory/go-bindings-win32/bindings/win32/ui/controls/dialogs"
)

// AVIBuildFilter wraps the raw AVIBuildFilterW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avibuildfilterw
func AVIBuildFilter(lpszFilter foundation.PWSTR, cbFilter int32, fSaving bool) error {
	_fSaving := foundation.BOOL(win32.Bool32(fSaving))
	return win32.HRESULTError(int32(mediamultimedia.AVIBuildFilterW(lpszFilter, cbFilter, _fSaving)))
}

// AVIBuildFilterA wraps the raw AVIBuildFilterA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avibuildfiltera
func AVIBuildFilterA(lpszFilter foundation.PSTR, cbFilter int32, fSaving bool) error {
	_fSaving := foundation.BOOL(win32.Bool32(fSaving))
	return win32.HRESULTError(int32(mediamultimedia.AVIBuildFilterA(lpszFilter, cbFilter, _fSaving)))
}

// AVIClearClipboard wraps the raw AVIClearClipboard call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-aviclearclipboard
func AVIClearClipboard() error {
	return win32.HRESULTError(int32(mediamultimedia.AVIClearClipboard()))
}

// AVIFileCreateStream wraps the raw AVIFileCreateStreamW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifilecreatestreamw
func AVIFileCreateStream(pfile *mediamultimedia.IAVIFile, ppavi **mediamultimedia.IAVIStream, psi *mediamultimedia.AVISTREAMINFOW) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileCreateStreamW(pfile, ppavi, psi)))
}

// AVIFileCreateStreamA wraps the raw AVIFileCreateStreamA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifilecreatestreama
func AVIFileCreateStreamA(pfile *mediamultimedia.IAVIFile, ppavi **mediamultimedia.IAVIStream, psi *mediamultimedia.AVISTREAMINFOA) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileCreateStreamA(pfile, ppavi, psi)))
}

// AVIFileEndRecord wraps the raw AVIFileEndRecord call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifileendrecord
func AVIFileEndRecord(pfile *mediamultimedia.IAVIFile) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileEndRecord(pfile)))
}

// AVIFileGetStream wraps the raw AVIFileGetStream call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifilegetstream
func AVIFileGetStream(pfile *mediamultimedia.IAVIFile, ppavi **mediamultimedia.IAVIStream, fccType uint32, lParam int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileGetStream(pfile, ppavi, fccType, lParam)))
}

// AVIFileInfo wraps the raw AVIFileInfoW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifileinfow
func AVIFileInfo(pfile *mediamultimedia.IAVIFile, pfi *mediamultimedia.AVIFILEINFOW, lSize int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileInfoW(pfile, pfi, lSize)))
}

// AVIFileInfoA wraps the raw AVIFileInfoA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifileinfoa
func AVIFileInfoA(pfile *mediamultimedia.IAVIFile, pfi *mediamultimedia.AVIFILEINFOA, lSize int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileInfoA(pfile, pfi, lSize)))
}

// AVIFileOpen wraps the raw AVIFileOpenW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifileopenw
func AVIFileOpen(ppfile **mediamultimedia.IAVIFile, szFile string, uMode uint32, lpHandler *win32.GUID) error {
	_szFile := win32.UTF16Ptr(szFile)
	return win32.HRESULTError(int32(mediamultimedia.AVIFileOpenW(ppfile, foundation.PWSTR(_szFile), uMode, lpHandler)))
}

// AVIFileOpenA wraps the raw AVIFileOpenA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifileopena
func AVIFileOpenA(ppfile **mediamultimedia.IAVIFile, szFile foundation.PSTR, uMode uint32, lpHandler *win32.GUID) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileOpenA(ppfile, szFile, uMode, lpHandler)))
}

// AVIFileReadData wraps the raw AVIFileReadData call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifilereaddata
func AVIFileReadData(pfile *mediamultimedia.IAVIFile, ckid uint32, lpData unsafe.Pointer, lpcbData *int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileReadData(pfile, ckid, lpData, lpcbData)))
}

// AVIFileWriteData wraps the raw AVIFileWriteData call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avifilewritedata
func AVIFileWriteData(pfile *mediamultimedia.IAVIFile, ckid uint32, lpData unsafe.Pointer, cbData int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIFileWriteData(pfile, ckid, lpData, cbData)))
}

// AVIGetFromClipboard wraps the raw AVIGetFromClipboard call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avigetfromclipboard
func AVIGetFromClipboard(lppf **mediamultimedia.IAVIFile) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIGetFromClipboard(lppf)))
}

// AVIMakeCompressedStream wraps the raw AVIMakeCompressedStream call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avimakecompressedstream
func AVIMakeCompressedStream(ppsCompressed **mediamultimedia.IAVIStream, ppsSource *mediamultimedia.IAVIStream, lpOptions *mediamultimedia.AVICOMPRESSOPTIONS, pclsidHandler *win32.GUID) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIMakeCompressedStream(ppsCompressed, ppsSource, lpOptions, pclsidHandler)))
}

// AVIMakeFileFromStreams wraps the raw AVIMakeFileFromStreams call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avimakefilefromstreams
func AVIMakeFileFromStreams(ppfile **mediamultimedia.IAVIFile, nStreams int32, papStreams **mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIMakeFileFromStreams(ppfile, nStreams, papStreams)))
}

// AVIMakeStreamFromClipboard wraps the raw AVIMakeStreamFromClipboard call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avimakestreamfromclipboard
func AVIMakeStreamFromClipboard(cfFormat uint32, hGlobal foundation.HANDLE, ppstream **mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIMakeStreamFromClipboard(cfFormat, hGlobal, ppstream)))
}

// AVIPutFileOnClipboard wraps the raw AVIPutFileOnClipboard call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-aviputfileonclipboard
func AVIPutFileOnClipboard(pf *mediamultimedia.IAVIFile) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIPutFileOnClipboard(pf)))
}

// AVISave wraps the raw AVISaveW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avisavew
func AVISave(szFile string, pclsidHandler *win32.GUID, lpfnCallback mediamultimedia.AVISAVECALLBACK, nStreams int32, pfile *mediamultimedia.IAVIStream, lpOptions *mediamultimedia.AVICOMPRESSOPTIONS) error {
	_szFile := win32.UTF16Ptr(szFile)
	return win32.HRESULTError(int32(mediamultimedia.AVISaveW(foundation.PWSTR(_szFile), pclsidHandler, lpfnCallback, nStreams, pfile, lpOptions)))
}

// AVISaveA wraps the raw AVISaveA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avisavea
func AVISaveA(szFile foundation.PSTR, pclsidHandler *win32.GUID, lpfnCallback mediamultimedia.AVISAVECALLBACK, nStreams int32, pfile *mediamultimedia.IAVIStream, lpOptions *mediamultimedia.AVICOMPRESSOPTIONS) error {
	return win32.HRESULTError(int32(mediamultimedia.AVISaveA(szFile, pclsidHandler, lpfnCallback, nStreams, pfile, lpOptions)))
}

// AVISaveOptionsFree wraps the raw AVISaveOptionsFree call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avisaveoptionsfree
func AVISaveOptionsFree(nStreams int32, plpOptions **mediamultimedia.AVICOMPRESSOPTIONS) error {
	return win32.HRESULTError(int32(mediamultimedia.AVISaveOptionsFree(nStreams, plpOptions)))
}

// AVISaveV wraps the raw AVISaveVW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avisavevw
func AVISaveV(szFile string, pclsidHandler *win32.GUID, lpfnCallback mediamultimedia.AVISAVECALLBACK, nStreams int32, ppavi **mediamultimedia.IAVIStream, plpOptions **mediamultimedia.AVICOMPRESSOPTIONS) error {
	_szFile := win32.UTF16Ptr(szFile)
	return win32.HRESULTError(int32(mediamultimedia.AVISaveVW(foundation.PWSTR(_szFile), pclsidHandler, lpfnCallback, nStreams, ppavi, plpOptions)))
}

// AVISaveVA wraps the raw AVISaveVA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avisaveva
func AVISaveVA(szFile foundation.PSTR, pclsidHandler *win32.GUID, lpfnCallback mediamultimedia.AVISAVECALLBACK, nStreams int32, ppavi **mediamultimedia.IAVIStream, plpOptions **mediamultimedia.AVICOMPRESSOPTIONS) error {
	return win32.HRESULTError(int32(mediamultimedia.AVISaveVA(szFile, pclsidHandler, lpfnCallback, nStreams, ppavi, plpOptions)))
}

// AVIStreamBeginStreaming wraps the raw AVIStreamBeginStreaming call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreambeginstreaming
func AVIStreamBeginStreaming(pavi *mediamultimedia.IAVIStream, lStart int32, lEnd int32, lRate int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamBeginStreaming(pavi, lStart, lEnd, lRate)))
}

// AVIStreamCreate wraps the raw AVIStreamCreate call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamcreate
func AVIStreamCreate(ppavi **mediamultimedia.IAVIStream, lParam1 int32, lParam2 int32, pclsidHandler *win32.GUID) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamCreate(ppavi, lParam1, lParam2, pclsidHandler)))
}

// AVIStreamEndStreaming wraps the raw AVIStreamEndStreaming call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamendstreaming
func AVIStreamEndStreaming(pavi *mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamEndStreaming(pavi)))
}

// AVIStreamGetFrameClose wraps the raw AVIStreamGetFrameClose call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamgetframeclose
func AVIStreamGetFrameClose(pg *mediamultimedia.IGetFrame) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamGetFrameClose(pg)))
}

// AVIStreamInfo wraps the raw AVIStreamInfoW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreaminfow
func AVIStreamInfo(pavi *mediamultimedia.IAVIStream, psi *mediamultimedia.AVISTREAMINFOW, lSize int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamInfoW(pavi, psi, lSize)))
}

// AVIStreamInfoA wraps the raw AVIStreamInfoA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreaminfoa
func AVIStreamInfoA(pavi *mediamultimedia.IAVIStream, psi *mediamultimedia.AVISTREAMINFOA, lSize int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamInfoA(pavi, psi, lSize)))
}

// AVIStreamOpenFromFile wraps the raw AVIStreamOpenFromFileW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamopenfromfilew
func AVIStreamOpenFromFile(ppavi **mediamultimedia.IAVIStream, szFile string, fccType uint32, lParam int32, mode uint32, pclsidHandler *win32.GUID) error {
	_szFile := win32.UTF16Ptr(szFile)
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamOpenFromFileW(ppavi, foundation.PWSTR(_szFile), fccType, lParam, mode, pclsidHandler)))
}

// AVIStreamOpenFromFileA wraps the raw AVIStreamOpenFromFileA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamopenfromfilea
func AVIStreamOpenFromFileA(ppavi **mediamultimedia.IAVIStream, szFile foundation.PSTR, fccType uint32, lParam int32, mode uint32, pclsidHandler *win32.GUID) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamOpenFromFileA(ppavi, szFile, fccType, lParam, mode, pclsidHandler)))
}

// AVIStreamRead wraps the raw AVIStreamRead call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamread
func AVIStreamRead(pavi *mediamultimedia.IAVIStream, lStart int32, lSamples int32, lpBuffer unsafe.Pointer, cbBuffer int32, plBytes *int32, plSamples *int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamRead(pavi, lStart, lSamples, lpBuffer, cbBuffer, plBytes, plSamples)))
}

// AVIStreamReadData wraps the raw AVIStreamReadData call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamreaddata
func AVIStreamReadData(pavi *mediamultimedia.IAVIStream, fcc uint32, lp unsafe.Pointer, lpcb *int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamReadData(pavi, fcc, lp, lpcb)))
}

// AVIStreamReadFormat wraps the raw AVIStreamReadFormat call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamreadformat
func AVIStreamReadFormat(pavi *mediamultimedia.IAVIStream, lPos int32, lpFormat unsafe.Pointer, lpcbFormat *int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamReadFormat(pavi, lPos, lpFormat, lpcbFormat)))
}

// AVIStreamSetFormat wraps the raw AVIStreamSetFormat call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamsetformat
func AVIStreamSetFormat(pavi *mediamultimedia.IAVIStream, lPos int32, lpFormat unsafe.Pointer, cbFormat int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamSetFormat(pavi, lPos, lpFormat, cbFormat)))
}

// AVIStreamWrite wraps the raw AVIStreamWrite call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamwrite
func AVIStreamWrite(pavi *mediamultimedia.IAVIStream, lStart int32, lSamples int32, lpBuffer unsafe.Pointer, cbBuffer int32, dwFlags uint32, plSampWritten *int32, plBytesWritten *int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamWrite(pavi, lStart, lSamples, lpBuffer, cbBuffer, dwFlags, plSampWritten, plBytesWritten)))
}

// AVIStreamWriteData wraps the raw AVIStreamWriteData call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-avistreamwritedata
func AVIStreamWriteData(pavi *mediamultimedia.IAVIStream, fcc uint32, lp unsafe.Pointer, cb int32) error {
	return win32.HRESULTError(int32(mediamultimedia.AVIStreamWriteData(pavi, fcc, lp, cb)))
}

// CapCreateCaptureWindow wraps the raw CapCreateCaptureWindowW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-capcreatecapturewindoww
func CapCreateCaptureWindow(lpszWindowName string, dwStyle uint32, x int32, y int32, nWidth int32, nHeight int32, hwndParent foundation.HWND, nID int32) foundation.HWND {
	_lpszWindowName := win32.UTF16Ptr(lpszWindowName)
	return mediamultimedia.CapCreateCaptureWindowW(foundation.PWSTR(_lpszWindowName), dwStyle, x, y, nWidth, nHeight, hwndParent, nID)
}

// CapGetDriverDescription wraps the raw CapGetDriverDescriptionW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-capgetdriverdescriptionw
func CapGetDriverDescription(wDriverIndex uint32, lpszName foundation.PWSTR, cbName int32, lpszVer foundation.PWSTR, cbVer int32) bool {
	return mediamultimedia.CapGetDriverDescriptionW(wDriverIndex, lpszName, cbName, lpszVer, cbVer) != 0
}

// CapGetDriverDescriptionA wraps the raw CapGetDriverDescriptionA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-capgetdriverdescriptiona
func CapGetDriverDescriptionA(wDriverIndex uint32, lpszName foundation.PSTR, cbName int32, lpszVer foundation.PSTR, cbVer int32) bool {
	return mediamultimedia.CapGetDriverDescriptionA(wDriverIndex, lpszName, cbName, lpszVer, cbVer) != 0
}

// CreateEditableStream wraps the raw CreateEditableStream call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-createeditablestream
func CreateEditableStream(ppsEditable **mediamultimedia.IAVIStream, psSource *mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.CreateEditableStream(ppsEditable, psSource)))
}

// DrawDibBegin wraps the raw DrawDibBegin call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibbegin
func DrawDibBegin(hdd uintptr, hdc graphicsgdi.HDC, dxDst int32, dyDst int32, lpbi *graphicsgdi.BITMAPINFOHEADER, dxSrc int32, dySrc int32, wFlags uint32) bool {
	return mediamultimedia.DrawDibBegin(hdd, hdc, dxDst, dyDst, lpbi, dxSrc, dySrc, wFlags) != 0
}

// DrawDibChangePalette wraps the raw DrawDibChangePalette call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibchangepalette
func DrawDibChangePalette(hdd uintptr, iStart int32, iLen int32, lppe *graphicsgdi.PALETTEENTRY) bool {
	return mediamultimedia.DrawDibChangePalette(hdd, iStart, iLen, lppe) != 0
}

// DrawDibClose wraps the raw DrawDibClose call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibclose
func DrawDibClose(hdd uintptr) bool {
	return mediamultimedia.DrawDibClose(hdd) != 0
}

// DrawDibDraw wraps the raw DrawDibDraw call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibdraw
func DrawDibDraw(hdd uintptr, hdc graphicsgdi.HDC, xDst int32, yDst int32, dxDst int32, dyDst int32, lpbi *graphicsgdi.BITMAPINFOHEADER, lpBits unsafe.Pointer, xSrc int32, ySrc int32, dxSrc int32, dySrc int32, wFlags uint32) bool {
	return mediamultimedia.DrawDibDraw(hdd, hdc, xDst, yDst, dxDst, dyDst, lpbi, lpBits, xSrc, ySrc, dxSrc, dySrc, wFlags) != 0
}

// DrawDibEnd wraps the raw DrawDibEnd call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibend
func DrawDibEnd(hdd uintptr) bool {
	return mediamultimedia.DrawDibEnd(hdd) != 0
}

// DrawDibRealize wraps the raw DrawDibRealize call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibrealize
func DrawDibRealize(hdd uintptr, hdc graphicsgdi.HDC, fBackground bool) uint32 {
	_fBackground := foundation.BOOL(win32.Bool32(fBackground))
	return mediamultimedia.DrawDibRealize(hdd, hdc, _fBackground)
}

// DrawDibSetPalette wraps the raw DrawDibSetPalette call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibsetpalette
func DrawDibSetPalette(hdd uintptr, hpal graphicsgdi.HPALETTE) bool {
	return mediamultimedia.DrawDibSetPalette(hdd, hpal) != 0
}

// DrawDibStart wraps the raw DrawDibStart call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibstart
func DrawDibStart(hdd uintptr, rate uint32) bool {
	return mediamultimedia.DrawDibStart(hdd, rate) != 0
}

// DrawDibStop wraps the raw DrawDibStop call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibstop
func DrawDibStop(hdd uintptr) bool {
	return mediamultimedia.DrawDibStop(hdd) != 0
}

// DrawDibTime wraps the raw DrawDibTime call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-drawdibtime
func DrawDibTime(hdd uintptr, lpddtime *mediamultimedia.DRAWDIBTIME) bool {
	return mediamultimedia.DrawDibTime(hdd, lpddtime) != 0
}

// DriverCallback wraps the raw DriverCallback call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-drivercallback
func DriverCallback(dwCallback uintptr, dwFlags uint32, hDevice mediamultimedia.HDRVR, dwMsg uint32, dwUser uintptr, dwParam1 uintptr, dwParam2 uintptr) bool {
	return mediamultimedia.DriverCallback(dwCallback, dwFlags, hDevice, dwMsg, dwUser, dwParam1, dwParam2) != 0
}

// EditStreamClone wraps the raw EditStreamClone call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamclone
func EditStreamClone(pavi *mediamultimedia.IAVIStream, ppResult **mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamClone(pavi, ppResult)))
}

// EditStreamCopy wraps the raw EditStreamCopy call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamcopy
func EditStreamCopy(pavi *mediamultimedia.IAVIStream, plStart *int32, plLength *int32, ppResult **mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamCopy(pavi, plStart, plLength, ppResult)))
}

// EditStreamCut wraps the raw EditStreamCut call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamcut
func EditStreamCut(pavi *mediamultimedia.IAVIStream, plStart *int32, plLength *int32, ppResult **mediamultimedia.IAVIStream) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamCut(pavi, plStart, plLength, ppResult)))
}

// EditStreamPaste wraps the raw EditStreamPaste call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreampaste
func EditStreamPaste(pavi *mediamultimedia.IAVIStream, plPos *int32, plLength *int32, pstream *mediamultimedia.IAVIStream, lStart int32, lEnd int32) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamPaste(pavi, plPos, plLength, pstream, lStart, lEnd)))
}

// EditStreamSetInfo wraps the raw EditStreamSetInfoW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamsetinfow
func EditStreamSetInfo(pavi *mediamultimedia.IAVIStream, lpInfo *mediamultimedia.AVISTREAMINFOW, cbInfo int32) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamSetInfoW(pavi, lpInfo, cbInfo)))
}

// EditStreamSetInfoA wraps the raw EditStreamSetInfoA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamsetinfoa
func EditStreamSetInfoA(pavi *mediamultimedia.IAVIStream, lpInfo *mediamultimedia.AVISTREAMINFOA, cbInfo int32) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamSetInfoA(pavi, lpInfo, cbInfo)))
}

// EditStreamSetName wraps the raw EditStreamSetNameW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamsetnamew
func EditStreamSetName(pavi *mediamultimedia.IAVIStream, lpszName string) error {
	_lpszName := win32.UTF16Ptr(lpszName)
	return win32.HRESULTError(int32(mediamultimedia.EditStreamSetNameW(pavi, foundation.PWSTR(_lpszName))))
}

// EditStreamSetNameA wraps the raw EditStreamSetNameA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-editstreamsetnamea
func EditStreamSetNameA(pavi *mediamultimedia.IAVIStream, lpszName foundation.PSTR) error {
	return win32.HRESULTError(int32(mediamultimedia.EditStreamSetNameA(pavi, lpszName)))
}

// GetOpenFileNamePreview wraps the raw GetOpenFileNamePreviewW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-getopenfilenameprevieww
func GetOpenFileNamePreview(lpofn *uicontrolsdialogs.OPENFILENAMEW) bool {
	return mediamultimedia.GetOpenFileNamePreviewW(lpofn) != 0
}

// GetOpenFileNamePreviewA wraps the raw GetOpenFileNamePreviewA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-getopenfilenamepreviewa
func GetOpenFileNamePreviewA(lpofn *uicontrolsdialogs.OPENFILENAMEA) bool {
	return mediamultimedia.GetOpenFileNamePreviewA(lpofn) != 0
}

// GetSaveFileNamePreview wraps the raw GetSaveFileNamePreviewW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-getsavefilenameprevieww
func GetSaveFileNamePreview(lpofn *uicontrolsdialogs.OPENFILENAMEW) bool {
	return mediamultimedia.GetSaveFileNamePreviewW(lpofn) != 0
}

// GetSaveFileNamePreviewA wraps the raw GetSaveFileNamePreviewA call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-getsavefilenamepreviewa
func GetSaveFileNamePreviewA(lpofn *uicontrolsdialogs.OPENFILENAMEA) bool {
	return mediamultimedia.GetSaveFileNamePreviewA(lpofn) != 0
}

// ICCompressorChoose wraps the raw ICCompressorChoose call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-iccompressorchoose
func ICCompressorChoose(hwnd foundation.HWND, uiFlags uint32, pvIn unsafe.Pointer, lpData unsafe.Pointer, pc *mediamultimedia.COMPVARS, lpszTitle foundation.PSTR) bool {
	return mediamultimedia.ICCompressorChoose(hwnd, uiFlags, pvIn, lpData, pc, lpszTitle) != 0
}

// ICInfo wraps the raw ICInfo call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-icinfo
func ICInfo(fccType uint32, fccHandler uint32, lpicinfo *mediamultimedia.ICINFO) bool {
	return mediamultimedia.ICInfo(fccType, fccHandler, lpicinfo) != 0
}

// ICInstall wraps the raw ICInstall call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-icinstall
func ICInstall(fccType uint32, fccHandler uint32, lParam foundation.LPARAM, szDesc foundation.PSTR, wFlags uint32) bool {
	return mediamultimedia.ICInstall(fccType, fccHandler, lParam, szDesc, wFlags) != 0
}

// ICRemove wraps the raw ICRemove call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-icremove
func ICRemove(fccType uint32, fccHandler uint32, wFlags uint32) bool {
	return mediamultimedia.ICRemove(fccType, fccHandler, wFlags) != 0
}

// ICSeqCompressFrame wraps the raw ICSeqCompressFrame call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-icseqcompressframe
func ICSeqCompressFrame(pc *mediamultimedia.COMPVARS, lpBits unsafe.Pointer, pfKey *foundation.BOOL, plSize *int32) unsafe.Pointer {
	return mediamultimedia.ICSeqCompressFrame(pc, 0, lpBits, pfKey, plSize)
}

// ICSeqCompressFrameStart wraps the raw ICSeqCompressFrameStart call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-icseqcompressframestart
func ICSeqCompressFrameStart(pc *mediamultimedia.COMPVARS, lpbiIn *graphicsgdi.BITMAPINFO) bool {
	return mediamultimedia.ICSeqCompressFrameStart(pc, lpbiIn) != 0
}

// JoyGetDevCaps wraps the raw JoyGetDevCapsW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/joystickapi/nf-joystickapi-joygetdevcapsw
func JoyGetDevCaps(uJoyID uintptr, pjc unsafe.Pointer, cbjc uint32) uint32 {
	return mediamultimedia.JoyGetDevCapsW(uJoyID, pjc, cbjc)
}

// JoySetCapture wraps the raw JoySetCapture call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/joystickapi/nf-joystickapi-joysetcapture
func JoySetCapture(hwnd foundation.HWND, uJoyID uint32, uPeriod uint32, fChanged bool) uint32 {
	_fChanged := foundation.BOOL(win32.Bool32(fChanged))
	return mediamultimedia.JoySetCapture(hwnd, uJoyID, uPeriod, _fChanged)
}

// MCIWndCreate wraps the raw MCIWndCreateW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-mciwndcreatew
func MCIWndCreate(hwndParent foundation.HWND, hInstance foundation.HINSTANCE, dwStyle uint32, szFile string) foundation.HWND {
	_szFile := win32.UTF16Ptr(szFile)
	return mediamultimedia.MCIWndCreateW(hwndParent, hInstance, dwStyle, foundation.PWSTR(_szFile))
}

// MCIWndRegisterClass wraps the raw MCIWndRegisterClass call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/vfw/nf-vfw-mciwndregisterclass
func MCIWndRegisterClass() bool {
	return mediamultimedia.MCIWndRegisterClass() != 0
}

// MciDriverNotify wraps the raw MciDriverNotify call with idiomatic Go types.
func MciDriverNotify(hwndCallback foundation.HANDLE, wDeviceID uint32, uStatus uint32) bool {
	return mediamultimedia.MciDriverNotify(hwndCallback, wDeviceID, uStatus) != 0
}

// MciFreeCommandResource wraps the raw MciFreeCommandResource call with idiomatic Go types.
func MciFreeCommandResource(wTable uint32) bool {
	return mediamultimedia.MciFreeCommandResource(wTable) != 0
}

// MciGetDeviceID wraps the raw MciGetDeviceIDW call with idiomatic Go types.
func MciGetDeviceID(pszDevice string) uint32 {
	_pszDevice := win32.UTF16Ptr(pszDevice)
	return mediamultimedia.MciGetDeviceIDW(foundation.PWSTR(_pszDevice))
}

// MciGetDeviceIDFromElementID wraps the raw MciGetDeviceIDFromElementIDW call with idiomatic Go types.
func MciGetDeviceIDFromElementID(dwElementID uint32, lpstrType string) uint32 {
	_lpstrType := win32.UTF16Ptr(lpstrType)
	return mediamultimedia.MciGetDeviceIDFromElementIDW(dwElementID, foundation.PWSTR(_lpstrType))
}

// MciGetErrorString wraps the raw MciGetErrorStringW call with idiomatic Go types.
func MciGetErrorString(mcierr uint32, pszText foundation.PWSTR, cchText uint32) bool {
	return mediamultimedia.MciGetErrorStringW(mcierr, pszText, cchText) != 0
}

// MciGetErrorStringA wraps the raw MciGetErrorStringA call with idiomatic Go types.
func MciGetErrorStringA(mcierr uint32, pszText foundation.PSTR, cchText uint32) bool {
	return mediamultimedia.MciGetErrorStringA(mcierr, pszText, cchText) != 0
}

// MciLoadCommandResource wraps the raw MciLoadCommandResource call with idiomatic Go types.
func MciLoadCommandResource(hInstance foundation.HANDLE, lpResName string, wType uint32) uint32 {
	_lpResName := win32.UTF16Ptr(lpResName)
	return mediamultimedia.MciLoadCommandResource(hInstance, foundation.PWSTR(_lpResName), wType)
}

// MciSendCommand wraps the raw MciSendCommandW call with idiomatic Go types.
func MciSendCommand(mciId uint32, uMsg uint32, dwParam1 uintptr, dwParam2 uintptr) uint32 {
	return mediamultimedia.MciSendCommandW(mciId, uMsg, dwParam1, dwParam2)
}

// MciSendString wraps the raw MciSendStringW call with idiomatic Go types.
func MciSendString(lpstrCommand string, lpstrReturnString foundation.PWSTR, uReturnLength uint32, hwndCallback foundation.HWND) uint32 {
	_lpstrCommand := win32.UTF16Ptr(lpstrCommand)
	return mediamultimedia.MciSendStringW(foundation.PWSTR(_lpstrCommand), lpstrReturnString, uReturnLength, hwndCallback)
}

// MciSetDriverData wraps the raw MciSetDriverData call with idiomatic Go types.
func MciSetDriverData(wDeviceID uint32, dwData uintptr) bool {
	return mediamultimedia.MciSetDriverData(wDeviceID, dwData) != 0
}

// MciSetYieldProc wraps the raw MciSetYieldProc call with idiomatic Go types.
func MciSetYieldProc(mciId uint32, fpYieldProc mediamultimedia.YIELDPROC, dwYieldData uint32) bool {
	return mediamultimedia.MciSetYieldProc(mciId, fpYieldProc, dwYieldData) != 0
}

// MmDrvInstall wraps the raw MmDrvInstall call with idiomatic Go types.
func MmDrvInstall(hDriver mediamultimedia.HDRVR, wszDrvEntry string, drvMessage mediamultimedia.DRIVERMSGPROC, wFlags uint32) uint32 {
	_wszDrvEntry := win32.UTF16Ptr(wszDrvEntry)
	return mediamultimedia.MmDrvInstall(hDriver, foundation.PWSTR(_wszDrvEntry), drvMessage, wFlags)
}

// MmTaskSignal wraps the raw MmTaskSignal call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmddk/nf-mmddk-mmtasksignal
func MmTaskSignal(h uint32) bool {
	return mediamultimedia.MmTaskSignal(h) != 0
}

// MmioInstallIOProc wraps the raw MmioInstallIOProcW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-mmioinstallioprocw
func MmioInstallIOProc(fccIOProc uint32, pIOProc mediamultimedia.LPMMIOPROC, dwFlags uint32) mediamultimedia.LPMMIOPROC {
	return mediamultimedia.MmioInstallIOProcW(fccIOProc, pIOProc, dwFlags)
}

// MmioOpen wraps the raw MmioOpenW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-mmioopenw
func MmioOpen(pszFileName foundation.PWSTR, pmmioinfo unsafe.Pointer, fdwOpen uint32) mediamultimedia.HMMIO {
	return mediamultimedia.MmioOpenW(pszFileName, pmmioinfo, fdwOpen)
}

// MmioRename wraps the raw MmioRenameW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-mmiorenamew
func MmioRename(pszFileName string, pszNewFileName string, pmmioinfo unsafe.Pointer, fdwRename uint32) uint32 {
	_pszFileName := win32.UTF16Ptr(pszFileName)
	_pszNewFileName := win32.UTF16Ptr(pszNewFileName)
	return mediamultimedia.MmioRenameW(foundation.PWSTR(_pszFileName), foundation.PWSTR(_pszNewFileName), pmmioinfo, fdwRename)
}

// MmioStringToFOURCC wraps the raw MmioStringToFOURCCW call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-mmiostringtofourccw
func MmioStringToFOURCC(sz string, uFlags uint32) uint32 {
	_sz := win32.UTF16Ptr(sz)
	return mediamultimedia.MmioStringToFOURCCW(foundation.PWSTR(_sz), uFlags)
}

// OpenDriver wraps the raw OpenDriver call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-opendriver
func OpenDriver(szDriverName string, szSectionName string, lParam2 foundation.LPARAM) mediamultimedia.HDRVR {
	_szDriverName := win32.UTF16Ptr(szDriverName)
	_szSectionName := win32.UTF16Ptr(szSectionName)
	return mediamultimedia.OpenDriver(foundation.PWSTR(_szDriverName), foundation.PWSTR(_szSectionName), lParam2)
}

// SndOpenSound wraps the raw SndOpenSound call with idiomatic Go types.
// https://learn.microsoft.com/windows/win32/api/mmiscapi/nf-mmiscapi-sndopensound
func SndOpenSound(EventName string, AppName string, Flags int32, FileHandle *foundation.HANDLE) int32 {
	_EventName := win32.UTF16Ptr(EventName)
	_AppName := win32.UTF16Ptr(AppName)
	return mediamultimedia.SndOpenSound(foundation.PWSTR(_EventName), foundation.PWSTR(_AppName), Flags, FileHandle)
}
